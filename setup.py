from setuptools import setup, find_packages

setup(
    name='fp_Pencarian_Pestisida',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        'Flask>=2.0.0',  
        'pandas>=1.0.0' 
    ],
)
